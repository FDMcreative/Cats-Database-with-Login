<h1>Homepage</h1>

<% users.forEach((user) => { %>
  <h1><%= user.username %></h1>
  <h2><%= user.email %></h2>
  <p><%= user.password %></p>
<% }) %>

<% cats.forEach((cat) => { %>
  <h1><%= cat.name %></h1>
  <h2><%= cat.age %></h2>
  <p><%= cat.description %></p>
<% }) %>
